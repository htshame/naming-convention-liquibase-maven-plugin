title: naming-convention-liquibase-maven-plugin v1.0.5
description: "A lightweight Maven plugin for validating XML Liquibase changeLog files against a customizable set of structural and content rules.<br>Enables teams to enforce Liquibase changeLog standards across codebases with zero runtime dependencies and minimal configuration overhead."
theme: jekyll-theme-slate
